T = int(input())

for tc in range(1 + T+1):
    N, M = map(int, input().split())

    matrix = [list(map(int, input().split())) for _ in range(N)]

    # 초기 변수 세팅
    ans = 0
    
    # matrix를 순회하면서(r, c)
        # tmp = matrix[r][c]
        # 그 칸을 기준으로 델타탐색
            # 좌표 찍기
            # 유효성 검사(리스트 범위 안의 좌표인가?)
            # tmp에 더해주기
        # 정답 갱신 시도

    
    # ans 출력

    